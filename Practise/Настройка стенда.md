Хост - Fedora 43.

Сначала установил виртуалки: Ubuntu, Debian и Windows 11.

На Ubuntu установил Wazuh: Indexer, Server и Dashboard. 

На Debian и Windows 11 агенты с помощью интерфейса Wazuh.

Чтобы виртуалки были доступны с внешней сети, добавить конфиг на хосте:
```
sudo tee /etc/nftables.conf > /dev/null <<EOF
#!/usr/sbin/nft -f

flush ruleset

table inet filter {
    # Трафик, направленный САМОМУ хосту Fedora
    chain input {
        type filter hook input priority 0; policy accept;
        
        # Разрешаем уже установленные соединения
        ct state established,related accept
        
        # Разрешаем петлю (localhost)
        iif "lo" accept
        
        # Разрешаем пинговать сам хост Fedora из локальной сети
        ip protocol icmp accept
    }

    # Трафик, проходящий СКВОЗЬ хост (Ноутбук -> VM)
    chain forward {
        type filter hook forward priority 0; policy drop;

        # 1. Разрешаем обратный трафик (ОБЯЗАТЕЛЬНО)
        ct state established,related accept

        # 2. Разрешаем ICMP (ping) проходящий сквозь хост
        # Это позволит ноутбуку получать ответы от VM
        ip protocol icmp accept

        # 3. Разрешаем доступ из локальной сети (enp4s0) к виртуальным машинам (virbr0)
        iifname "enp4s0" oifname "virbr0" ip daddr 192.168.122.0/24 accept

        # 4. Разрешаем виртуальным машинам (virbr0) выходить в интернет (enp4s0)
        iifname "virbr0" oifname "enp4s0" accept

        # 5. Разрешаем виртуальным машинам общаться друг с другом
        iifname "virbr0" oifname "virbr0" accept
    }

    chain output {
        type filter hook output priority 0; policy accept;
    }
}

# Таблица для NAT (интернет для VM)
table ip nat {
    chain postrouting {
        type nat hook postrouting priority 100; policy accept;
        ip saddr 192.168.122.0/24 oifname "enp4s0" masquerade
    }
}
EOF

# Выклчить firewalld
sudo systemctl stop firewalld

# Применяем конфигурацию
sudo nft -f /etc/nftables.conf
```

Также на Windows виртуалке нужно сделать:
```
netsh advfirewall firewall add rule name="Allow Ping" dir=in action=allow protocol=icmpv4 powershell Get-NetConnectionProfile | Set-NetConnectionProfile -NetworkCategory Private
```

